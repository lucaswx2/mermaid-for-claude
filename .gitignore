node_modules/
*.log
.DS_Store
# The Windows console width helper is compiled into the cache directory outside the plugin (ADR-0008);
# the plugin ships hooks/console-width.cs and never a binary.
*.exe
